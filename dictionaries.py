# Dictionaries are basically synonymous to objects in js

dictionary = {
  "key": "value",
  "a": "value for a",
}

print(dictionary["a"])

# Add to dict
dictionary["b"] = "value for b"

print(dictionary["b"])

# Delete from dict

del dictionary["b"]
